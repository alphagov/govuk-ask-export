---
pipelines:
  cabinet-office:
    destinations:
      - google_drive
    only_completed: true
    fields:
      - id
      - region
      - name
      - email
      - phone
      - share_video
  third-party:
    destinations:
      - google_drive
    only_completed: true
    fields:
      - id
      - region
      - question
      - share_video
  gcs-public-questions:
    destinations:
      - aws_s3
    only_completed: true
    fields:
      - submission_time
      - region
      - question

---
pipelines:
  cabinet-office:
    destinations:
      - google_drive
    only_completed: true
    fields:
      - id
      - region
      - name
      - email
      - phone
      - question_format
  third-party:
    destinations:
      - google_drive
    only_completed: true
    fields:
      - id
      - region
      - question
      - question_format
  gcs-public-questions:
    destinations:
      - aws_s3
    only_completed: true
    fields:
      - submission_time
      - region
      - question
